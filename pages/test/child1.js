import React from 'react';
import Child11 from './child11';

const Child1 = ({ onChange }) => {
  
  return (
    <div>
      <Child11 onChange={onChange} />
    </div>
  );
};

export default Child1;
